<div class="container">
<h3>Hi {{welcomeMessage}}!, You are in the city of {{cityName}}. {{tagline}}</h3>
  <div class="row">
    {{#each imageArray}}
       <div class="col-md-6">
          <div class="thumbnail">
          <img src="/images/{{../cityName}}{{this}}.jpg" 
          width="400" 
          class="img-circle"/>
          </div>
       </div>
    {{/each}}
  </div>
</div>
